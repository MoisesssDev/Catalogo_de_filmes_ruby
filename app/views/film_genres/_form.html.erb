<%= form_with(model: film_genre) do |form| %>
  <% if film_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(film_genre.errors.count, "error") %> prohibited this film_genre from being saved:</h2>

      <ul>
        <% film_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
